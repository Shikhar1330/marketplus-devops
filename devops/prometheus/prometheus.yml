global:
  scrape_interval: 10s
scrape_configs:
  - job_name: 'marketplus-backend'
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:5000']
        labels:
          app: 'marketplus'
