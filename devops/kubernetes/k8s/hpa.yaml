apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: marketplus-backend-hpa
  namespace: marketplus
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: marketplus-backend-blue
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
