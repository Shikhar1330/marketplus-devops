const {isValidEmail,isStrongPassword,isValidRole}=require('../utils/validators');test('valid email',()=>{expect(isValidEmail('admin@marketplus.com')).toBe(true);expect(isValidEmail('wrong')).toBe(false)});test('strong password',()=>{expect(isStrongPassword('Admin@123')).toBe(true);expect(isStrongPassword('123')).toBe(false)});test('valid role',()=>{expect(isValidRole('ADMIN')).toBe(true);expect(isValidRole('HACKER')).toBe(false)});
